## Project setup
```
1. npm install
2. npm run server
3. cd client (to change directory)
4. npm install
5. npm run client
6. Open browser and run application on "localhost:8080"

## Mongodb setup
1. Install Mongo db
2. Create database with name "employeeData" or pass database URI in config/keys.js